{
  "name": "sciredev",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ./apps/web dev",
    "build": "pnpm --filter ./apps/web build",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "dev:playground": "pnpm --filter ./packages/exercise-engine dev:playground",
    "brand:playground": "python3 -m http.server 4174 --directory docs",
    "test": "echo 'Error: no test runner configured' && exit 1"
  },
  "devDependencies": {
    "@schpet/linear-cli": "^2.6.0",
    "nuxt": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:"
  }
}
